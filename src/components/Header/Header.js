import React from 'react';
import LogoText from "../svg/LogoTxt";

const Header = () => (
  <div className="text-center mg-md">
      <LogoText />
      <span className="header--text mg-sm">회원가입</span>
  </div>
);

export default Header;